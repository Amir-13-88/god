# 🖥️ ترمینال چیه و چطور بازش کنم؟

**ترمینال** یک برنامه‌ست که بهت اجازه می‌ده دستورات متنی به کامپیوترت بدی. مثل یک ماشین‌حساب برای برنامه‌نویس‌ها!

---

## 🪟 اگه ویندوز داری

### روش ۱: Command Prompt (ساده‌ترین)

1. **دکمه Start** (شروع) رو بزن (پایین سمت چپ صفحه)
2. تایپ کن: `cmd`
3. روی **"Command Prompt"** یا **"خط فرمان"** کلیک کن
4. یک پنجره سیاه باز می‌شه — این همون ترمیناله!

### روش ۲: PowerShell (پیشنهادی)

1. **دکمه Start** رو بزن
2. تایپ کن: `powershell`
3. روی **"Windows PowerShell"** کلیک کن
4. یک پنجره آبی باز می‌شه

### روش ۳: از VS Code (راحت‌ترین!)

اگه **VS Code** نصب داری:

1. **VS Code** رو باز کن
2. پوشه پروژه رو باز کن (File > Open Folder)
3. دکمه‌های **Ctrl + `** (کنترل و علامت `) رو همزمان بزن
4. ترمینال پایین صفحه باز می‌شه!

**یا:**

1. در VS Code، از منوی بالا: **Terminal > New Terminal**
2. ترمینال باز می‌شه

---

## 🍎 اگه مک (Mac) داری

### روش ۱: Terminal (ساده‌ترین)

1. دکمه‌های **Command + Space** رو بزن (Spotlight)
2. تایپ کن: `terminal`
3. **Enter** بزن
4. ترمینال باز می‌شه!

### روش ۲: از Finder

1. **Finder** رو باز کن
2. برو به: **Applications > Utilities**
3. روی **"Terminal"** دابل‌کلیک کن

### روش ۳: از VS Code

1. **VS Code** رو باز کن
2. پوشه پروژه رو باز کن
3. دکمه‌های **Ctrl + `** رو بزن
4. ترمینال باز می‌شه!

---

## 🐧 اگه لینوکس داری

### روش ۱: Keyboard Shortcut

دکمه‌های **Ctrl + Alt + T** رو همزمان بزن

### روش ۲: از Menu

1. روی منوی برنامه‌ها کلیک کن
2. دنبال **"Terminal"** بگرد
3. روش کلیک کن

### روش ۳: از VS Code

1. **VS Code** رو باز کن
2. پوشه پروژه رو باز کن
3. دکمه‌های **Ctrl + `** رو بزن

---

## 🎯 الان چیکار کنم؟

### قدم ۱: ترمینال رو باز کن

یکی از روش‌های بالا رو استفاده کن و ترمینال رو باز کن.

### قدم ۲: برو به پوشه پروژه

فرض می‌کنم پروژه‌ات در پوشه‌ای به اسم `leitner-app` ذخیره شده.

**در ویندوز:**
```bash
cd C:\Users\YourName\Documents\leitner-app
```

**در مک/لینوکس:**
```bash
cd ~/Documents/leitner-app
```

(به جای `YourName`، اسم کاربریت رو بنویس)

### قدم ۳: نصب وابستگی‌ها

این دستور رو تایپ کن و **Enter** بزن:

```bash
npm install
```

صبر کن تا نصب تموم بشه (ممکنه ۱-۲ دقیقه طول بکشه).

### قدم ۴: اجرای برنامه

این دستور رو تایپ کن و **Enter** بزن:

```bash
npm run dev
```

یک پیام می‌بینی که می‌گه:

```
  VITE v5.0.0  ready in 500 ms

  ➜  Local:   http://localhost:5173/
```

### قدم ۵: باز کردن برنامه

مرورگر رو باز کن و برو به:

```
http://localhost:5173
```

✅ **تمام! برنامه باز شد!**

---

## ❓ سوال: VS Code چیه؟

**VS Code** یک ویرایشگر کد رایگانه که کار با ترمینال رو خیلی راحت‌تر می‌کنه.

### نصب VS Code:

1. برو به: https://code.visualstudio.com/
2. روی **"Download"** کلیک کن
3. فایل نصب رو اجرا کن
4. تمام!

### چرا VS Code خوبه؟

- ✅ ترمینال داخلی داره
- ✅ رنگی کردن کد
- ✅ پیشنهاد خودکار
- ✅ رایگان
- ✅ خیلی محبوب

---

## 🆘 اگه نمی‌تونی ترمینال باز کنی

### راه‌حل جایگزین: استفاده از VS Code

1. **VS Code** رو نصب کن (از لینک بالا)
2. **VS Code** رو باز کن
3. از منوی بالا: **File > Open Folder**
4. پوشه پروژه رو انتخاب کن
5. از منوی بالا: **Terminal > New Terminal**
6. ترمینال پایین صفحه باز می‌شه!

---

## 💡 نکته مهم

**ترمینال فقط یک ابزاره!** مهم اینه که دستورات درست رو وارد کنی.

دستوراتی که نیاز داری:

```bash
# نصب وابستگی‌ها
npm install

# اجرای برنامه
npm run dev

# توقف برنامه
# (Ctrl + C رو بزن)
```

---

## 🎯 خلاصه

1. **ترمینال رو باز کن** (از روش‌های بالا)
2. **برو به پوشه پروژه** (`cd path/to/project`)
3. **نصب وابستگی‌ها** (`npm install`)
4. **اجرای برنامه** (`npm run dev`)
5. **باز کردن در مرورگر** (http://localhost:5173)

---

## 🆘 هنوز مشکل داری؟

اگه نمی‌تونی ترمینال رو باز کنی یا دستورات کار نمی‌کنن:

1. **اسکرین‌شات** از صفحه‌ات بگیر
2. بهم نشون بده
3. کمکت می‌کنم!

یا:

1. بگو چه سیستم‌عاملی داری (ویندوز/مک/لینوکس)
2. بگو دقیقاً کجا گیر کردی
3. راهنماییت می‌کنم

---

موفق باشی! 💪
