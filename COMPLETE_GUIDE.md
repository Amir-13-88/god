# 📖 راهنمای کامل از صفر تا صد - برای مبتدیان مطلق

**این راهنما فرض می‌کنه شما هیچی از برنامه‌نویسی نمی‌دونید و همه‌چیز رو از اول توضیح می‌ده.**

---

## 🎯 هدف ما چیه؟

می‌خوایم برنامه "لایتنر زبان" رو روی کامپیوترت اجرا کنیم و اگه خواستی، بک‌اند Firebase رو هم راه‌اندازی کنیم.

---

# 📚 بخش اول: آماده‌سازی کامپیوتر

---

## مرحله ۱: نصب Node.js

**Node.js چیه؟** یک برنامه‌ست که به کامپیوترت اجازه می‌ده کدهای جاوااسکریپت رو اجرا کنه.

### قدم ۱.۱: دانلود Node.js

1. مرورگر (Chrome, Firefox, Edge) رو باز کن
2. برو به این آدرس: **https://nodejs.org**
3. دو تا دکمه سبز می‌بینی:
   - **LTS** (پیشنهادی) ← این رو بزن
   - Current ← این نه
4. روی دکمه **LTS** کلیک کن
5. فایل دانلود می‌شه (حدود ۳۰ مگابایت)

### قدم ۱.۲: نصب Node.js

**در ویندوز:**

1. فایل دانلود شده رو پیدا کن (معمولاً در پوشه Downloads)
2. اسمش چیزی مثل `node-v20.x.x-x64.msi` هست
3. روش **دابل‌کلیک** کن
4. پنجره نصب باز می‌شه
5. **Next** بزن
6. **I accept the terms** رو تیک بزن ← **Next**
7. همه رو **Next** بزن تا برسی به **Install**
8. روی **Install** کلیک کن
9. صبر کن تا نصب تموم بشه
10. **Finish** رو بزن

**در مک:**

1. فایل `.pkg` رو باز کن
2. **Continue** بزن تا نصب تموم بشه

### قدم ۱.۳: تست نصب

1. ترمینال رو باز کن (راهنمای ترمینال رو ببین)
2. این دستور رو تایپ کن:
   ```bash
   node --version
   ```
3. **Enter** بزن
4. باید چیزی مثل این ببینی:
   ```
   v20.11.0
   ```
5. اگه عدد دیدی، یعنی Node.js نصب شده! ✅

---

## مرحله ۲: نصب VS Code (ویرایشگر کد)

**VS Code چیه؟** یک برنامه که کدها رو توش می‌نویسیم و ترمینال هم داره.

### قدم ۲.۱: دانلود VS Code

1. برو به: **https://code.visualstudio.com**
2. روی دکمه بزرگ **"Download"** کلیک کن
3. نسخه مناسب سیستم‌عاملت رو انتخاب کن:
   - ویندوز: **Windows**
   - مک: **Mac**
   - لینوکس: **Linux**
4. فایل دانلود می‌شه

### قدم ۲.۲: نصب VS Code

**در ویندوز:**

1. فایل `VSCodeUserSetup-x64-xxx.exe` رو باز کن
2. **I accept** رو تیک بزن ← **Next**
3. همه رو **Next** بزن
4. تیک **"Add to PATH"** رو بزن (مهمه!)
5. **Install** بزن
6. صبر کن ← **Finish**

**در مک:**

1. فایل `.zip` رو باز کن
2. VS Code رو به پوشه Applications بکش
3. تمام!

### قدم ۲.۳: باز کردن VS Code

1. VS Code رو باز کن
2. یک صفحه آبی/مشکی می‌بینی
3. آماده‌ست! ✅

---

# 📁 بخش دوم: آماده‌سازی پروژه

---

## مرحله ۳: دانلود پروژه

فرض می‌کنم فایل‌های پروژه رو داری (همین فایل‌هایی که الان داری می‌خونی).

### قدم ۳.۱: ساخت پوشه پروژه

**در ویندوز:**

1. **File Explorer** رو باز کن
2. برو به جایی که می‌خوای پروژه باشه (مثلاً Documents)
3. کلیک راست کن ← **New** ← **Folder**
4. اسمش رو بذار: `leitner-app`
5. Enter بزن

**در مک:**

1. **Finder** رو باز کن
2. برو به Documents
3. کلیک راست ← **New Folder**
4. اسم: `leitner-app`

### قدم ۳.۲: کپی فایل‌های پروژه

تمام فایل‌های پروژه رو کپی کن و در پوشه `leitner-app` بذار.

فایل‌هایی که باید داشته باشی:
- `package.json`
- `vite.config.js`
- `index.html`
- پوشه `src`
- پوشه `public`
- و بقیه فایل‌ها

### قدم ۳.۳: باز کردن پروژه در VS Code

1. **VS Code** رو باز کن
2. از منوی بالا: **File** ← **Open Folder** (یا **Open** در مک)
3. پوشه `leitner-app` رو انتخاب کن
4. **Select Folder** رو بزن
5. حالا باید سمت چپ، لیست فایل‌ها رو ببینی

---

## مرحله ۴: باز کردن ترمینال در VS Code

### قدم ۴.۱: باز کردن ترمینال

1. در VS Code، از منوی بالا: **Terminal** ← **New Terminal**
2. یا دکمه‌های **Ctrl + `** (کنترل و علامت `) رو بزن
3. پایین صفحه، یک پنجره سیاه/آبی باز می‌شه ← این ترمیناله!

### قدم ۴.۲: بررسی مسیر

در ترمینال، باید ببینی که مسیرت درسته. باید چیزی مثل این ببینی:

**ویندوز:**
```
C:\Users\YourName\Documents\leitner-app>
```

**مک/لینوکس:**
```
yourname@computer leitner-app %
```

اگه مسیرت درسته، برو به مرحله بعد. ✅

اگه نه، برو به پوشه پروژه:
```bash
cd path/to/leitner-app
```

---

# 🚀 بخش سوم: اجرای برنامه (بدون Firebase)

---

## مرحله ۵: نصب وابستگی‌ها

**وابستگی چیه؟** کتابخانه‌هایی که برنامه نیاز داره.

### قدم ۵.۱: اجرای دستور نصب

در ترمینال، این دستور رو تایپ کن:

```bash
npm install
```

**Enter** بزن.

### قدم ۵.۲: صبر کن

- پیام‌هایی می‌بینی که داره دانلود می‌کنه
- ممکنه ۱-۳ دقیقه طول بکشه
- صبر کن تا تموم بشه
- آخرش باید ببینی:
  ```
  added 234 packages in 45s
  ```

✅ تموم شد!

---

## مرحله ۶: اجرای برنامه

### قدم ۶.۱: اجرای دستور dev

در ترمینال، این دستور رو تایپ کن:

```bash
npm run dev
```

**Enter** بزن.

### قدم ۶.۲: پیام موفقیت

باید چیزی مثل این ببینی:

```
  VITE v5.0.0  ready in 500 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
  ➜  press h + enter to show help
```

### قدم ۶.۳: باز کردن برنامه

1. مرورگر (Chrome, Firefox) رو باز کن
2. در نوار آدرس، تایپ کن:
   ```
   http://localhost:5173
   ```
3. **Enter** بزن

✅ **برنامه باز شد!** باید صفحه اصلی برنامه رو ببینی!

---

## مرحله ۷: استفاده از برنامه

حالا می‌تونی:

- 🔍 در دیکشنری جستجو کنی
- ➕ واژه به مجموعه‌ات اضافه کنی
- 📚 مرور لایتنر انجام بدی
- ⚙️ تنظیمات رو تغییر بدی

### توقف برنامه

اگه خواستی برنامه رو متوقف کنی:

1. برو به ترمینال
2. دکمه‌های **Ctrl + C** رو بزن
3. اگه پرسید **Terminate batch job?**، تایپ کن: `Y` و Enter بزن

---

# 🔥 بخش چهارم: راه‌اندازی Firebase (اختیاری)

**اگه فقط می‌خوای برنامه کار کنه، همین‌جا تموم شد!** ✅

اما اگه می‌خوای داده‌هات در ابر ذخیره بشه و از چند دستگاه استفاده کنی، ادامه بده.

---

## مرحله ۸: ساخت پروژه Firebase

### قدم ۸.۱: رفتن به Firebase Console

1. مرورگر رو باز کن
2. برو به: **https://console.firebase.google.com**
3. با حساب گوگل (Gmail) وارد شو

### قدم ۸.۲: ساخت پروژه جدید

1. روی دکمه **"Add project"** کلیک کن
2. یک صفحه باز می‌شه با عنوان **"Create a project"**
3. در فیلد **"Project name"**، تایپ کن:
   ```
   leitner-language-app
   ```
4. دکمه **"Continue"** رو بزن (پایین صفحه)

### قدم ۸.۳: Google Analytics

1. صفحه بعدی می‌پرسه: **"Enable Google Analytics?"**
2. سوئیچ رو **خاموش** کن (نیاز نیست)
3. **"Continue"** رو بزن
4. **"Create project"** رو بزن
5. صبر کن (حدود ۳۰ ثانیه)
6. **"Continue"** رو بزن

✅ پروژه ساخته شد!

---

## مرحله ۹: فعال‌سازی Authentication

### قدم ۹.۱: رفتن به Authentication

1. در منوی سمت چپ، دنبال **"Authentication"** بگرد
2. روش کلیک کن
3. صفحه اصلی Authentication باز می‌شه
4. روی دکمه **"Get started"** کلیک کن

### قدم ۹.۲: فعال‌سازی Google Sign-in

1. صفحه **"Sign-in method"** باز می‌شه
2. لیستی از روش‌های ورود می‌بینی
3. روی **"Google"** کلیک کن
4. یک پنجره باز می‌شه
5. سوئیچ **"Enable"** رو **روشن** کن (آبی بشه)
6. در فیلد **"Project support email"**، ایمیل خودت رو انتخاب کن
7. پایین صفحه، **"Save"** رو بزن

✅ Google Sign-in فعال شد!

### قدم ۹.۳: فعال‌سازی Email/Password

1. دوباره به صفحه **"Sign-in method"** برگرد
2. روی **"Email/Password"** کلیک کن
3. سوئیچ **"Enable"** رو **روشن** کن
4. **"Save"** رو بزن

✅ Email/Password فعال شد!

---

## مرحله ۱۰: ساخت Firestore Database

### قدم ۱۰.۱: رفتن به Firestore

1. در منوی سمت چپ، دنبال **"Firestore Database"** بگرد
2. روش کلیک کن
3. صفحه اصلی Firestore باز می‌شه
4. روی دکمه **"Create database"** کلیک کن

### قدم ۱۰.۲: انتخاب موقعیت

1. پنجره **"Create Database"** باز می‌شه
2. در dropdown **"Cloud Firestore location"**، یکی رو انتخاب کن:
   - **europe-west1** (آلمان) ← پیشنهادی برای ایران
   - یا **europe-west3** (بلژیک)
   - یا **asia-south1** (هند)
3. **"Next"** رو بزن

### قدم ۱۰.۳: انتخاب حالت امنیتی

1. صفحه بعدی می‌پرسه: **"Secure rules for Cloud Firestore"**
2. گزینه **"Start in test mode"** رو انتخاب کن
3. **"Next"** رو بزن
4. **"Enable"** رو بزن
5. صبر کن تا Database ساخته بشه

✅ Firestore آماده‌ست!

---

## مرحله ۱۱: گرفتن پیکربندی Firebase

### قدم ۱۱.۱: رفتن به تنظیمات پروژه

1. در منوی سمت چپ، بالا، روی آیکون **چرخ‌دنده (⚙️)** کلیک کن
2. **"Project settings"** رو انتخاب کن

### قدم ۱۱.۲: اضافه کردن Web App

1. صفحه تنظیمات باز می‌شه
2. پایین صفحه، بخش **"Your apps"** رو ببین
3. سه تا آیکون می‌بینی:
   - 📱 iOS
   - 🤖 Android
   - **</>** ← این وب هست!
4. روی آیکون **</>** کلیک کن

### قدم ۱۱.۳: ثبت Web App

1. پنجره **"Add Firebase to your web app"** باز می‌شه
2. در فیلد **"App nickname"**، تایپ کن:
   ```
   Leitner Web
   ```
3. تیک **"Also set up Firebase Hosting"** رو **نزن** (نیاز نیست)
4. **"Register app"** رو بزن

### قدم ۱۱.۴: کپی کردن پیکربندی

1. صفحه بعدی، یک بلوک کد می‌بینی
2. عنوانش هست: **"Or, add the SDK"**
3. یک بلوک کد جاوااسکریپت می‌بینی که شبیه اینه:

```javascript
const firebaseConfig = {
  apiKey: "AIzaSyB1xY2z3A4bC5dE6fG7hI8jK9lM0nO1pQ",
  authDomain: "leitner-language-app.firebaseapp.com",
  projectId: "leitner-language-app",
  storageBucket: "leitner-language-app.appspot.com",
  messagingSenderId: "123456789012",
  appId: "1:123456789012:web:abcdef1234567890"
};
```

4. **کل این بلوک رو انتخاب کن** (از `const` تا `};`)
5. **کپی کن** (Ctrl+C یا Command+C)

✅ پیکربندی کپی شد!

---

## مرحله ۱۲: جایگزینی پیکربندی در کد

### قدم ۱۲.۱: باز کردن فایل پیکربندی

1. برو به **VS Code**
2. در سمت چپ، لیست فایل‌ها رو ببین
3. این مسیر رو دنبال کن:
   ```
   src
   └── lib
       └── firebase.ts
   ```
4. روی **`firebase.ts`** کلیک کن تا باز بشه

### قدم ۱۲.۲: پیدا کردن بخش پیکربندی

در فایل، این بخش رو پیدا کن (خطوط ۴ تا ۱۱):

```typescript
const firebaseConfig = {
  apiKey: "YOUR_API_KEY_HERE",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
```

### قدم ۱۲.۳: جایگزینی مقادیر

**حالا مقادیر رو با مقادیر واقعی جایگزین کن:**

مثلاً اگه پیکربندی Firebase تو اینه:

```javascript
const firebaseConfig = {
  apiKey: "AIzaSyB1xY2z3A4bC5dE6fG7hI8jK9lM0nO1pQ",
  authDomain: "leitner-language-app.firebaseapp.com",
  projectId: "leitner-language-app",
  storageBucket: "leitner-language-app.appspot.com",
  messagingSenderId: "123456789012",
  appId: "1:123456789012:web:abcdef1234567890"
};
```

کد VS Code رو به این تغییر بده:

```typescript
const firebaseConfig = {
  apiKey: "AIzaSyB1xY2z3A4bC5dE6fG7hI8jK9lM0nO1pQ",
  authDomain: "leitner-language-app.firebaseapp.com",
  projectId: "leitner-language-app",
  storageBucket: "leitner-language-app.appspot.com",
  messagingSenderId: "123456789012",
  appId: "1:123456789012:web:abcdef1234567890"
};
```

### قدم ۱۲.۴: ذخیره فایل

- دکمه‌های **Ctrl+S** (یا Command+S در مک) رو بزن
- فایل ذخیره می‌شه

✅ پیکربندی جایگزین شد!

---

## مرحله ۱۳: تست Firebase

### قدم ۱۳.۱: اجرای برنامه

1. در VS Code، ترمینال رو باز کن (Ctrl+`)
2. اگه برنامه قبلاً داره اجرا می‌شه، **Ctrl+C** بزن و متوقفش کن
3. دوباره اجرا کن:
   ```bash
   npm run dev
   ```
4. مرورگر رو باز کن: **http://localhost:5173**

### قدم ۱۳.۲: تست حساب کاربری

1. در برنامه، روی تب **"برنامه"** (پایین صفحه) کلیک کن
2. روی تب **"حساب کاربری"** کلیک کن
3. باید صفحه حساب کاربری رو ببینی

### قدم ۱۳.۳: تست ورود با Google

1. روی دکمه **"ورود با Google"** کلیک کن
2. یک پنجره باز می‌شه
3. حساب گوگل خودت رو انتخاب کن
4. اگه پرسید **"Choose an account"**، حساب خودت رو انتخاب کن
5. اگه پرسید **"Sign in to..."**، **"Continue"** رو بزن
6. باید برگردی به برنامه و ببینی که وارد شدی! ✅

### قدم ۱۳.۴: تست همگام‌سازی

1. چند واژه به مجموعه‌ات اضافه کن
2. روی دکمه **"آپلود در ابر"** کلیک کن
3. باید پیام **"داده‌ها با موفقیت در ابر ذخیره شد"** رو ببینی ✅

### قدم ۱۳.۵: بررسی در Firebase Console

1. برو به **Firebase Console**
2. روی **"Firestore Database"** کلیک کن
3. باید یک collection به نام **"users"** ببینی
4. داخلش، یک document با ID کاربرت هست
5. داخلش، داده‌های شما ذخیره شده ✅

---

# 🎉 تبریک!

تبریک می‌گم! شما با موفقیت:

✅ برنامه رو اجرا کردی  
✅ Firebase رو راه‌اندازی کردی  
✅ احراز هویت رو فعال کردی  
✅ همگام‌سازی ابری رو تست کردی  

حالا یک برنامه کامل داری با بک‌اند! 🚀

---

# 🆘 رفع مشکلات

---

## مشکل ۱: `npm: command not found`

**علت:** Node.js نصب نیست

**راه‌حل:**
1. برو به مرحله ۱
2. Node.js رو نصب کن
3. کامپیوتر رو ری‌استارت کن
4. دوباره امتحان کن

---

## مشکل ۲: `npm install` خطا می‌ده

**علت:** مشکل در اتصال اینترنت یا فایل‌های پروژه

**راه‌حل:**
1. مطمئن شو اینترنت داری
2. مطمئن شو همه فایل‌های پروژه رو داری
3. پوشه `node_modules` رو پاک کن (اگه هست)
4. دوباره `npm install` بزن

---

## مشکل ۳: `npm run dev` کار نمی‌کنه

**علت:** وابستگی‌ها نصب نشدن

**راه‌حل:**
1. اول `npm install` رو بزن
2. صبر کن تا تموم بشه
3. بعد `npm run dev` بزن

---

## مشکل ۴: صفحه سفید در مرورگر

**علت:** برنامه درست اجرا نشده

**راه‌حل:**
1. ترمینال رو چک کن (باید پیام VITE رو ببینی)
2. مرورگر رو رفرش کن (F5)
3. اگه باز هم سفیده، Console مرورگر رو باز کن (F12) و خطاها رو ببین

---

## مشکل ۵: Firebase کار نمی‌کنه

**علت:** پیکربندی اشتباهه

**راه‌حل:**
1. برو به Firebase Console
2. Project Settings رو باز کن
3. پیکربندی رو دوباره کپی کن
4. در VS Code، فایل `firebase.ts` رو باز کن
5. مقادیر رو دوباره جایگزین کن
6. ذخیره کن و برنامه رو رفرش کن

---

# 📞 نیاز به کمک داری؟

اگه به مشکل برخوردی:

1. **اسکرین‌شات** از صفحه‌ات بگیر
2. **متن خطا** رو کپی کن
3. بهم بگو دقیقاً کجا گیر کردی

کمکت می‌کنم! 💪

---

موفق باشی! 🎊
